<%- include("head", { title: "Diary Example" }) -%>
<%- include("header") -%>
    <main class="form-signin w-100 m-auto">
    <h1 class="h3 mb-3 fw-normal">일기장</h1> <!--can't finish '게시판' in jslint-->

    <table class="table table-striped">
        <thead>
            <tr>
                <th>No.</th>
                <th>Title</th>
                <th>Content</th>
            </tr>
        </thead>
        <tbody>
            <% for(const diaries of diary){ %> <!-- [const name].[*DATABASE COLUMN NAME*, not the name in index.ts] -->
            <tr>
                <td><%= diaries.d_id %></td>
                <td><%= diaries.d_title %></td>
                <td><%= diaries.d_contents %></td>
            </tr>
            <% } %>
        </tbody>
    </table>

    </main>
    <footer class="d-flex flex-wrap justify-content-between align-items-center py-3 my-4 border-top">
        <p class="col-md-4 mb-0 text-muted">&copy; 2022 Web Programming Group C</p>

        <ul class="nav col-md-4 justify-content-end">
            <li class="nav-item"><a href="/" class="nav-link px-2 text-muted">Home</a></li>
        <% if (loggedin) { %>
            <li class="nav-item"><a href="/logout" class="nav-link px-2 text-muted">Logout</a></li>
        <% } %>
        </ul>
    </footer>
</div>
<%- include('foot') -%>

